# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
pydantic-settings>=2.7.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
psycopg2-binary>=2.9.10

# Celery
celery>=5.4.0
redis>=5.2.0

# MinIO
minio>=7.2.0

# Data processing
pandas>=2.2.0
polars>=1.0.0
openpyxl>=3.1.0
pyyaml>=6.0.0
pyarrow>=15.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Cleaning engine — fuzzy matching
jellyfish>=1.0.0
thefuzz>=0.22.0

# Observability — Prometheus metrics + JSON structured logs
prometheus-client>=0.21.0
python-json-logger>=3.2.0

# LLM
langchain-core>=0.3.33
langchain-anthropic>=0.3.5
langchain-openai>=0.3.0
langchain-ollama>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.60

# PDF generation
weasyprint>=63.0
jinja2>=3.1.0

# Auth
# python-jose removed: unused (all JWT handling uses PyJWT) and its only role
# was pulling `ecdsa`, which carries an unpatched Minerva timing-attack
# advisory (PYSEC-2026-1325) with no fixed release.
PyJWT[crypto]>=2.8.0

# HTTP client
httpx>=0.28.0

# Billing
stripe>=11.0.0

# Observability
sentry-sdk[fastapi]>=2.19.0
scikit-learn==1.6.1

# SAP connector — optional, required when SAP_CONNECTOR=rfc (default)
# pyrfc>=3.1.0  # Install separately: requires SAP NW RFC SDK. See docs/sap-connector.md.
