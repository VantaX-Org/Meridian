# Local dev secrets — never commit this file
# These values are for local development only.
# For production, use: wrangler secret put <NAME>

# Admin login credentials
ADMIN_EMAIL=admin@meridian.local

# SHA-256 of "admin123" — change this!
# To generate: echo -n "yourpassword" | openssl dgst -sha256 | awk '{print $2}'
ADMIN_PASSWORD_HASH=240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9

# JWT signing secret — use a long random string in production
JWT_SECRET=dev-jwt-secret-change-in-production-use-wrangler-secret-put
