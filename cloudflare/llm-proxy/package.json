{
  "name": "meridian-llm-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Meridian LLM proxy Cloudflare Worker — fronts Ollama Cloud for all customer deployments",
  "main": "dist/worker.js",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260814.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}