[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    perf: benchmark tests, opt-in via `pytest -m perf`, not run by default
addopts = -m "not perf"
